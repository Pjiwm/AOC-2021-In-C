#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../helpers/file_reader.h"

void day13() {
    const size_t LENGTH = 1;
    char* input[LENGTH];
    // file_to_array("inputs/day1.txt", input);
}